#include "armstrong_numbers.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

/**
 *
 */
bool is_armstrong_number(int candidate) {
       
    int sum = 0;
    const char *str = candidate;
    printf("%ld", strlen(str));
    for (int i = 0; i < ; i++) {
        
    }
        
    return false;
       
}
